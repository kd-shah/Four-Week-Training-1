import { NumberFormatsPipe } from './number-formats.pipe';

describe('NumberFormatsPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberFormatsPipe();
    expect(pipe).toBeTruthy();
  });
});
